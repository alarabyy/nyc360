import { DragScroll } from './drag-scroll.directive';

describe('DragScroll', () => {
  it('should create an instance', () => {
    const directive = new DragScroll();
    expect(directive).toBeTruthy();
  });
});
